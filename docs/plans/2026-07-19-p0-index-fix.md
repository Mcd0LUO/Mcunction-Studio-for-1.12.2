# Plan: P0 索引正确性修复

**日期**: 2026-07-19  
**复杂度**: 中  
**模块**: `LineIndex` / `IndexedStore` / `DataLoader` / `DynamicDocManager` / `MinecraftExtractor` / yaml extractor

## 目标

1. LineIndex 主键与清理键统一为 `uri.toString()`
2. `clearLines` / `clearFile` 覆盖 Tag / FakePlayer，Function **按行**移除
3. 同行多 entry 用 append，避免互相覆盖；YAML 与内置共存
4. 多行编辑按变更范围从 `startLine` 重解析到文件尾
5. 单文件 reload 前完整 clear

## 验收

- 单元测试：多 Tag、clear 后幽灵消失、function 多引用按行删
- `npm run test:baseline` 仍通过
- 编译 + lint 无 error
