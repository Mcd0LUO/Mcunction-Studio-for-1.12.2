# Plan: P1 加载与补全热路径性能

**日期**: 2026-07-19  
**复杂度**: 中

## 改动

| 项 | 做法 |
|----|------|
| 默认并发 | 100 → **16** |
| 单飞行 | `loadData` 互斥，避免重叠全量 |
| mtime 增量 | `forceFull=false` 时跳过未改文件 |
| UI | 完成只 statusBar，不弹 Information |
| init | `await loadData` |
| 补全 | items/blocks 缓存；blocks 用 BlockNameMap 描述；去掉 teams log |

## 验收

- `npm run bench:perf`：冷启动与 **warm 增量** 均有数字
- 编译 / index / baseline 测试通过
