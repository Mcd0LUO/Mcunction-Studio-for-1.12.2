# 项目状态 — 2026-06-21

## 版本: v2.0.0-dev (纯 DSL 架构)

## 架构

```
extension.ts → DslCompletionProvider → CompletionEngine (DSL)
                                     → CompletionHelpers (selector/NBT)
                                     → MacroCompletionProvider (宏)
             → Signature (DSL SignatureEngine)
             → DataLoader / Hover / Definition / CodeLens

src/ (56 个 .ts 文件)
├── dsl/            (9 文件)   命令 DSL 框架
├── commands/       (9 文件)   48+ 命令声明
├── completionProvider/ (3 文件) DslProvider + Helpers + Macro
├── core/           (7 文件)   DataLoader / Signature / Hover / Definition / ...
├── macro/          (8 文件)   宏系统
├── resources/      (8 文件)   Minecraft 游戏数据
├── utils/          (9 文件)   工具函数
└── extension.ts   (1 文件)   入口
```

## 已删除
- ❌ `completionProvider/command/*` (42 文件 — 手写 if/else 补全逻辑)
- ❌ `completionProvider/Base.ts` (476 行抽象基类)
- ❌ `completionProvider/Minecraft.ts` (空壳单例)
- ❌ `core/CommandRegistry.ts` (动态 import 注册表)

## 新增
- ✅ DSL 构造器 (RootCommand / SubCommand / Flag / Option / Positional)
- ✅ DslCommandRegistry (声明式注册)
- ✅ CompletionEngine (命令树 → CompletionItem 自动生成)
- ✅ SignatureEngine (命令树 → SignatureHelp 自动生成)
- ✅ 48+ 命令 DSL 定义 (7 文件)
- ✅ CompletionHelpers (独立工具函数，替代 Base.ts)
- ✅ resources/ (8 文件 — 拆分 EnumLib)
- ✅ 4 个 DataLoader bug 修复

## 编译
- TypeScript strict: true, 零错误
- 源文件: 75 → 56 个 (-25%)
- 代码行: ~8000 → ~5000 (估算，-37%)
